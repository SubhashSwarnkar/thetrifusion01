import Page from "views/AIEstimatorPage";
import { pageMetadata } from "lib/seoConfig";

export const metadata = pageMetadata("/estimate");

export default function RoutePage() {
  return <Page />;
}
