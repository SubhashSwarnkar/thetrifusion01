import Page from "views/TimelineCalculatorPage";
import { pageMetadata } from "lib/seoConfig";

export const metadata = pageMetadata("/timeline");

export default function RoutePage() {
  return <Page />;
}
